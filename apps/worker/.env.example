# ===========================================
# SUPABASE CONFIGURATION (Required)
# ===========================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api

# Your Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase SERVICE ROLE key (required for server-side operations)
# WARNING: Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ===========================================
# ANTHROPIC API (Required for AI agent)
# ===========================================
# Get your key from: https://console.anthropic.com/settings/keys

ANTHROPIC_API_KEY=sk-ant-api03-...

# ===========================================
# WORKER CONFIGURATION
# ===========================================

# Unique identifier for this worker instance
WORKER_ID=worker-1

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Polling interval in seconds (how often to check for new tasks)
POLL_INTERVAL=2

# ===========================================
# DIGITAL OCEAN DEPLOYMENT NOTES
# ===========================================
# 
# Option 1: App Platform (Recommended)
# ------------------------------------
# 1. Go to DigitalOcean App Platform: https://cloud.digitalocean.com/apps
# 2. Create App > Select GitHub repo > Select apps/worker folder
# 3. Configure as "Worker" (not Web Service)
# 4. Add environment variables above
# 5. Set Run Command: python main.py
#
# Option 2: Droplet with Docker
# ------------------------------------
# 1. Create a Droplet (Ubuntu 22.04, minimum 2GB RAM)
# 2. SSH into droplet and install Docker:
#    curl -fsSL https://get.docker.com | sh
# 3. Clone your repo and cd into apps/worker
# 4. Create .env file with variables above
# 5. Build and run:
#    docker build -t miniemployee-worker .
#    docker run -d --env-file .env --name worker miniemployee-worker
#
# Option 3: Container Registry + Kubernetes
# ------------------------------------
# 1. Push to DigitalOcean Container Registry
# 2. Deploy to DOKS cluster
# See: do-app-spec.yaml for app spec
